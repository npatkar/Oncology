<h1>New Email Template</h1>

<% form_for(@template) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial=>'f', :object=>f %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', templates_path %>
